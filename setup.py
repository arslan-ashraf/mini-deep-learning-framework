from setuptools import setup, find_packages

setup(name="mini-deep-learning-framework", version="1.0", packages=find_packages())

# in the terminal, cd into project root directory, in this case "used-car-project" 
# and from that directory, run "pip install -e ."